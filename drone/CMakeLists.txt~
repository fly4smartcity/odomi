cmake_minimum_required(VERSION 2.8.3)
project(drone)

find_package(catkin REQUIRED COMPONENTS roscpp rospy)

catkin_package()

add_executable(drone_parrot src/drone_parrot.cpp)
target_link_libraries(drone_parrot ${catkin_LIBRARIES})

